import React, { memo } from 'react';
import { View, Text } from 'react-native';

const _{{pascalCase name}} = ({}) => {
  return (
    <View>
      <Text>{{pascalCase name}}</Text>
    </View>
  );
};

export const {{pascalCase name}} = memo(_{{pascalCase name}});